Form: {{ item.form_name }} -- Submission # {{ item.sid }}

{% for field, value in item %}
{{ field }}:
  {% if value is iterable %}
    {% for subs in value %}
--Index: {{ loop.index }}
      {% if subs is iterable %}
        {% for key, sub in subs %}
-- --{{ key }}: {{ sub }}
        {% endfor %}
      {% else %}
--{{ subs }}
      {% endif %}
    {% endfor %}
  {% else %}
--{{ value }}
  {% endif %}
{% endfor %}
