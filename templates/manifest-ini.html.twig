[package]
submitter_email = {{ manifest.submitter_email }}
content_model = {{ manifest.content_model }}
parent_collection = {{ manifest.parent_collection }}
{% if manifest.register_doi|length > 0 %}
register_doi = {{ manifest.register_doi }}
{% endif %}
{% if manifest.ip_embargo|length > 0 %}

[ip_embargo]
ip_expiry: {{ manifest.ip_embargo }}
{% endif %}
{% if manifest.scholar_expiry|length > 0 %}

[scholar_embargo]
scholar_expiry: {{ manifest.scholar_expiry }}
scholar_type: {{ manifest.scholar_type }}
{% endif %}
